//Global Variables
float ballStart, ballSize;
void setup() { 
  //size(900, 600);
  fullScreen();
  ballStart = width *1/2;
  ballSize = width*1/32;
  ellipse(ballStart,ballStart,ballSize,ballSize);
  
  //GUI();
}

void draw () {

}
